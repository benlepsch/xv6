Ben Lepsch btl9zu
For checkpoint one, ben wrote the dumppagetable function.
for checkpoint two, ben modified the growproc() function and the copyuvm() function.

Ian Le iml7uk
For checkpoint one, ian wrote the isphysicalpagefree and getpagetableentry functions.
For checkpoint two, ian wrote the new page fault trap function.


Throughout both checkpoints we both helped each other debug issues with the code.